using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpeedItem : Item
{
    float playerSpeed;
    float playerDamage;

   // protected override void ExecuteItem()//PlayerController player)
    //{
        //playerSpeed = player.speed;
        //playerDamage = player.damage;

        ////if(player.health < (player.maxHealth / 2.0f))
        ////{
        ////    player.speed += speedStat;
        ////    player.damage *= damageStat;

        ////    player.StartTimer(playerSpeed, time, stateOne);
        ////    player.StartTimer(playerDamage, time, stateTwo);

        ////    PlayerController.collectedAmount += 1;
        ////    base.ExecuteItem(player);
        ////}



        //if (GameController.Health < (GameController.MaxHealth / 2.0f))
        //{
        //    player.speed += speedStat;
        //    player.damage *= damageStat;

        //    player.StartTimer(playerSpeed, time, stateOne);
        //    player.StartTimer(playerDamage, time, stateTwo);

        //    PlayerController.collectedAmount += 1;
        //    base.ExecuteItem(player);
        //}



        //else
        //{
        //    //TODO: ADD TO INVENTORY
        //}
    //}
}
